{
    "version": 4,
    "configurePresets": [
        {
            "name": "user-prefs",
            "description": "CMake-level user preferences (build system, tool paths). Kconfig-owned options are now in config/user-kconfig.conf (the file the build reads for local Kconfig overrides) — copy config/user-kconfig.conf.TEMPLATE to create it.",
            "hidden": true,
            "cacheVariables": {
                "CMAKE_BUILD_TYPE": "Debug",
                "BUILD_VERSION": "2.0.0.0",
                "BUILD_VERBOSE": "OFF",
                "LEDTREES_IDF_COMPONENTS_PATH": "CHANGE_ME_PATH_TO_ledtrees-idf-components_CLONE"
            }
        }
    ],

    "buildPresets": [
        {
            "cleanFirst": false,
            "configuration": "Debug",
            "hidden": true,
            "name": "base-user",
            "verbose": false
        }
    ]
}
